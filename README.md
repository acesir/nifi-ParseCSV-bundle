# nifi-ParseCSV-bundle

Process contains the ability:
* parse various formats of CSVs (RTF, Fixed delimiters, Excel, etc..)
* mask headers
* encrypt headers
* tokenize mask/source values and output into desired stores
